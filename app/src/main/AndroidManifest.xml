<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gyh.tourguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pingyaoActivity"
            android:label="@string/pingyaogucheng" />
        <activity
            android:name=".mianshanActivity"
            android:label="@string/mianshanfengjingqu" />
        <activity
            android:name=".qiaojiaActivity"
            android:label="@string/qiaojiadayuan" />
        <activity
            android:name=".wangjiaActivity"
            android:label="@string/wangjiadayuan"></activity>
    </application>

</manifest>